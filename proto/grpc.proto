syntax = "proto3";

package proto;

service Jjogaegi {
    rpc Run(RunRequest) returns (RunResponse) {}
}

message RunRequest {
    string input = 1;
    string parser = 2;
    string formatter = 3;
}

message RunResponse {
    string output = 1;
}